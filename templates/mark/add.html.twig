{% extends 'base.html.twig' %}

{% block title %}{{title}}{% endblock %}

{% block body %}

<div class="container">
    <br>
    <h1>{{title}}</h1>
    <br>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

    <ul class="list-group">
    </ul>
</div>
{% endblock %}
